<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Sub Sanctuary Game Servers</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --bg: #0a0612;
      --card: #140d22;
      --accent: #9b5cff;
      --accent-soft: rgba(155, 92, 255, 0.18);
      --text-main: #f3eaff;
      --text-muted: #b9a8d9;
      --border-subtle: #2a1f3d;
      --danger: #ff4f6b;
      --warning: #ffcf4d;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    body {
      background: radial-gradient(circle at top, #1c1233 0, #0a0612 55%);
      color: var(--text-main);
      min-height: 100vh;
    }

    /* LOGO IN TOP RIGHT */
    .logo-container {
      position: absolute;
      top: 20px;
      right: 25px;
    }

    .logo-container img {
      height: 70px;
      width: auto;
      opacity: 0.95;
      transition: opacity 0.15s ease;
    }

    .logo-container img:hover {
      opacity: 1;
    }

    .page {
      max-width: 1100px;
      margin: 0 auto;
      padding: 2.5rem 1.5rem 3.5rem;
    }

    header {
      text-align: center;
      margin-bottom: 2.5rem;
    }

    header h1 {
      font-size: 2.4rem;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    header p {
      margin-top: 0.75rem;
      color: var(--text-muted);
      font-size: 0.98rem;
    }

    .tagline {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      margin-top: 1rem;
      padding: 0.3rem 0.8rem;
      border-radius: 999px;
      background: rgba(0, 0, 0, 0.35);
      border: 1px solid var(--border-subtle);
      font-size: 0.8rem;
      color: var(--text-muted);
    }

    .tagline span.dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: var(--accent);
      box-shadow: 0 0 8px var(--accent);
    }

    .layout {
      display: grid;
      grid-template-columns: minmax(0, 2fr) minmax(0, 1.2fr);
      gap: 1.75rem;
    }

    @media (max-width: 900px) {
      .layout {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    section {
      background: linear-gradient(145deg, #140d22, #0b0714);
      border-radius: 1rem;
      border: 1px solid var(--border-subtle);
      padding: 1.5rem 1.4rem 1.6rem;
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.55);
    }

    section h2 {
      font-size: 1.2rem;
      margin-bottom: 0.9rem;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: #e8d9ff;
    }

    section p {
      color: var(--text-muted);
      font-size: 0.9rem;
      margin-bottom: 0.9rem;
      line-height: 1.5;
    }

    .servers-list {
      display: flex;
      flex-direction: column;
      gap: 0.9rem;
      margin-top: 0.6rem;
    }

    .server-card {
      border-radius: 0.9rem;
      border: 1px solid var(--border-subtle);
      background: radial-gradient(circle at top left, rgba(155, 92, 255, 0.15), #0f0a19);
      padding: 0.9rem 1rem;
      display: grid;
      grid-template-columns: minmax(0, 2.2fr) minmax(0, 1.4fr);
      gap: 0.6rem 1rem;
      align-items: center;
    }

    @media (max-width: 650px) {
      .server-card {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    .server-main {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .server-name {
      font-weight: 600;
      font-size: 1rem;
    }

    .server-desc {
      font-size: 0.85rem;
      color: var(--text-muted);
    }

    .server-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
      font-size: 0.78rem;
      margin-top: 0.2rem;
    }

    .pill {
      padding: 0.18rem 0.55rem;
      border-radius: 999px;
      border: 1px solid var(--border-subtle);
      background: rgba(0, 0, 0, 0.35);
      color: var(--text-muted);
    }

    .pill-accent {
      border-color: var(--accent);
      background: var(--accent-soft);
      color: #f3eaff;
    }

    .pill-warning {
      border-color: var(--warning);
      background: rgba(255, 207, 77, 0.12);
      color: #ffe9b3;
    }

    .pill-danger {
      border-color: var(--danger);
      background: rgba(255, 79, 107, 0.12);
      color: #ffcdd2;
    }

    .server-connection {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      font-size: 0.82rem;
      color: var(--text-muted);
    }

    .server-connection code {
      background: rgba(0, 0, 0, 0.55);
      border-radius: 0.4rem;
      padding: 0.25rem 0.45rem;
      border: 1px solid var(--border-subtle);
      font-size: 0.8rem;
      color: #f3eaff;
    }

    .steps {
      margin-top: 0.4rem;
      list-style: decimal;
      padding-left: 1.2rem;
      font-size: 0.88rem;
      color: var(--text-muted);
    }

    .steps li {
      margin-bottom: 0.35rem;
    }

    .steps strong {
      color: #e8d9ff;
      font-weight: 600;
    }

    .discord-card {
      display: flex;
      flex-direction: column;
      gap: 0.8rem;
    }

    .discord-highlight {
      border-radius: 0.9rem;
      border: 1px solid #5865f2;
      background: radial-gradient(circle at top left, rgba(88, 101, 242, 0.18), #0b0714);
      padding: 0.9rem 1rem;
      display: flex;
      flex-direction: column;
      gap: 0.4rem;
    }

    .discord-name {
      font-weight: 600;
      font-size: 1rem;
      color: #e8d9ff;
    }

    .discord-link {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.4rem;
      margin-top: 0.3rem;
      padding: 0.55rem 0.9rem;
      border-radius: 999px;
      border: none;
      background: #7a5bff;
      color: #fff;
      font-size: 0.9rem;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      box-shadow: 0 10px 25px rgba(122, 91, 255, 0.45);
      transition: transform 0.08s ease-out, box-shadow 0.08s ease-out, background 0.08s ease-out;
    }

    .discord-link:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 30px rgba(122, 91, 255, 0.6);
      background: #6d4df0;
    }

    footer {
      margin-top: 2.2rem;
      text-align: center;
      font-size: 0.78rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>

  <!-- LOGO TOP RIGHT -->
  <a class="logo-container" href="https://yourlink.com">
    <img src="https://yourdomain.com/logo.png" alt="Your Logo">
  </a>

  <div class="page">
    <!-- (Everything else stays the same — server list, Discord, etc.) -->
  </div>

</body>
</html>
